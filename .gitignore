# Private planning material — symlinked from sibling repo, never committed to Core
/.planning
/base

# macOS
.DS_Store

# Go build / test artifacts
bin/
dist/
*.exe
*.test
*.out
coverage.out
coverage.html
profile.out
cpu.prof
mem.prof
vendor/

# Go module / build caches (these live under $GOPATH/pkg or $GOCACHE by default,
# but listed defensively in case of a project-local override).
.gocache/

# IDEs
.idea/
.vscode/
*.swp
*.swo
*~

# Postgres / AGE
*.dump
pg_data/
pgdata/

# Logs
*.log

# Local environment
.env
.env.local
.env.*.local

# Test artifacts
junit.xml
.testcontainers/
# DR test artifacts (Plan 00-04 / Wave 3)
/dr-writer
tests/dr/_*.csv
tests/dr/_*.json

# Frontend (web/) build output — Phase 1+; pre-emptively listed
web/node_modules/
web/dist/
web/.vite/
