// neksur-cli — admin / operations command-line tool.
//
// Phase 0 stub. M1+ subcommands target: schema introspection, policy
// dry-run evaluation, catalog adapter probes, migration controls. The
// CLI consumes the same internal/* packages as the server binary.
package main

import "fmt"

func main() {
	fmt.Println("Neksur CLI (placeholder — Phase 0 stub; M1+ will add admin subcommands).")
}
